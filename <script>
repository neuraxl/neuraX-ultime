
    function sendMessage() {
      const userInput = document.getElementById("userInput");
      const chatLog = document.getElementById("chatLog");
      const message = userInput.value.trim();
      if (message) {
        chatLog.value += "👤: " + message + "\n";
        userInput.value = "";

        // Réponse fictive de l'IA pour la démo
        const response = "🧠: Réponse de NeuraX en cours de synchronisation...\n";
        chatLog.value += response;

        // Fait parler NeuraX avec la voix synthétique
        speak("Réponse de NeuraX en cours de synchronisation.");
        
        // Scroll en bas automatiquement
        chatLog.scrollTop = chatLog.scrollHeight;
      }
    }
  </script>
</body>
</html>

